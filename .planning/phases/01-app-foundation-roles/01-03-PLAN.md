---
phase: 01-app-foundation-roles
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified: [src/components/app-shell.tsx, src/lib/auth/guards.ts, src/middleware.ts, src/app/catalog/page.tsx, src/app/librarian/dashboard/page.tsx, src/app/layout.tsx, tests/auth/guards.test.ts, tests/e2e/auth-role-access.spec.ts, playwright.config.ts]
autonomous: true
requirements: [AUTH-04, AUTH-01, AUTH-02, AUTH-03]
user_setup: []
must_haves:
  truths:
    - "Students see Catalog, My Borrowings placeholder, and Logout navigation."
    - "Librarians see Dashboard, Catalog Management placeholder, Borrow Requests placeholder, and Logout navigation."
    - "A student who directly opens a librarian-only page is redirected to /catalog with an access message."
    - "Role access is enforced server-side, not only by hidden links."
    - "Focused auth/role-access tests verify the Phase 1 flows."
  artifacts:
    - path: "src/components/app-shell.tsx"
      provides: "Role-aware navigation shell"
      min_lines: 30
    - path: "src/middleware.ts"
      provides: "Route-level role protection or session-aware redirects"
      min_lines: 20
    - path: "tests/e2e/auth-role-access.spec.ts"
      provides: "Browser tests for auth and role access"
      min_lines: 40
    - path: "tests/auth/guards.test.ts"
      provides: "Role guard unit tests"
      min_lines: 20
  key_links:
    - from: "src/middleware.ts"
      to: "src/lib/auth/session.ts"
      via: "Reads signed session cookie for protected routes"
      pattern: "library_session"
    - from: "src/app/catalog/page.tsx"
      to: "src/components/app-shell.tsx"
      via: "Student page renders role-aware navigation"
      pattern: "AppShell"
    - from: "tests/e2e/auth-role-access.spec.ts"
      to: "/librarian/dashboard"
      via: "Direct navigation access-denied test"
      pattern: "access"
---

<objective>
Complete Phase 1 role-aware access, navigation, route protection, and verification tests.

Purpose: Ensure users see and can access only the actions allowed for their role, satisfying AUTH-04 and validating the auth flows from Plan 02.
Output: Role-aware app shell, protected routes, access-denied redirect behavior, and focused tests for student/librarian flows.
Decision coverage: D-07 student direct access to librarian-only pages redirects to the catalog with an access message; D-08 student navigation shows Catalog, My Borrowings placeholder, and Logout; D-09 librarian navigation shows Dashboard, Catalog Management placeholder, Borrow Requests placeholder, and Logout; D-12 focused auth/role-access tests verify the behavior.
</objective>

<execution_context>
@C:/Users/hiday/WebstormProjects/zephyra-agenticai-library/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/hiday/WebstormProjects/zephyra-agenticai-library/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-app-foundation-roles/01-CONTEXT.md
@.planning/phases/01-app-foundation-roles/01-RESEARCH.md
@.planning/phases/01-app-foundation-roles/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add role-aware navigation shell</name>
  <files>src/components/app-shell.tsx, src/app/catalog/page.tsx, src/app/librarian/dashboard/page.tsx, src/app/layout.tsx</files>
  <read_first>src/lib/auth/session.ts, src/app/catalog/page.tsx, src/app/librarian/dashboard/page.tsx, .planning/phases/01-app-foundation-roles/01-CONTEXT.md</read_first>
  <action>Create AppShell that accepts the current user role and renders navigation. For STUDENT, show Catalog, My Borrowings placeholder, and Logout. For LIBRARIAN, show Dashboard, Catalog Management placeholder, Borrow Requests placeholder, and Logout. Use restrained school-library dashboard styling from globals.css. Wire catalog and dashboard pages through AppShell and keep placeholder destinations non-functional or clearly marked as coming later.</action>
  <verify>npm run build</verify>
  <acceptance_criteria>
    - AppShell renders Catalog, My Borrowings, and Logout for STUDENT.
    - AppShell renders Dashboard, Catalog Management, Borrow Requests, and Logout for LIBRARIAN.
    - Role pages import and render AppShell.
  </acceptance_criteria>
  <done>Navigation reflects the active user's role.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce protected routes and access-denied redirects</name>
  <files>src/lib/auth/guards.ts, src/middleware.ts, src/app/catalog/page.tsx, src/app/librarian/dashboard/page.tsx, tests/auth/guards.test.ts</files>
  <read_first>src/lib/auth/session.ts, src/lib/auth/guards.ts, src/app/actions/auth.ts</read_first>
  <action>Add route protection so anonymous users visiting protected pages go to /login. Add librarian-route protection so STUDENT users visiting /librarian/dashboard or other /librarian paths are redirected to /catalog?access=denied. Add a visible access message on /catalog when search param access=denied is present. Implement guard tests for redirect targets and permitted roles. Prefer a single source of truth in guards.ts for role route rules; middleware and page-level checks should not duplicate conflicting logic.</action>
  <verify>npm test -- tests/auth/guards.test.ts and npm run build</verify>
  <acceptance_criteria>
    - guards.ts exports role route helpers with tests.
    - src/middleware.ts protects /catalog and /librarian routes from anonymous access where feasible.
    - Student access to /librarian/dashboard redirects to /catalog?access=denied.
    - /catalog displays an access-denied message when access=denied is present.
  </acceptance_criteria>
  <done>Role access is enforced beyond hidden navigation links.</done>
</task>

<task type="auto">
  <name>Task 3: Add focused browser verification for auth and role access</name>
  <files>playwright.config.ts, tests/e2e/auth-role-access.spec.ts, package.json</files>
  <read_first>.env.example, prisma/seed.ts, src/app/actions/auth.ts, src/lib/auth/guards.ts</read_first>
  <action>Configure Playwright for local browser tests. Add an auth-role-access spec that starts from a clean seeded database or unique test user data and verifies: student registration lands on /catalog, student logout returns to /login, seeded librarian login lands on /librarian/dashboard, librarian logout returns to /login, and student direct navigation to /librarian/dashboard redirects to /catalog with an access message. Ensure the npm script test:e2e runs this spec.</action>
  <verify>npm run test:e2e</verify>
  <acceptance_criteria>
    - playwright.config.ts exists and targets the local Next.js dev server.
    - tests/e2e/auth-role-access.spec.ts covers student register/login/logout, librarian login/logout, and student denied librarian access.
    - package.json test:e2e runs Playwright.
  </acceptance_criteria>
  <done>Focused browser tests prove Phase 1 auth and role access behavior.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run lint
- [ ] npm run build
- [ ] npm test
- [ ] npm run test:e2e
- [ ] Verify manually if needed: student denied access to /librarian/dashboard returns to /catalog with visible message.
</verification>

<success_criteria>
- Student and librarian navigation match Phase 1 decisions.
- Server-side role protection exists.
- Student direct access to librarian-only routes redirects to catalog with an access message.
- Auth and role access flows are covered by focused tests.
- Phase 1 requirements AUTH-01 through AUTH-04 are all covered by executable plans.
</success_criteria>

<output>
After completion, create `.planning/phases/01-app-foundation-roles/01-03-SUMMARY.md`
</output>
