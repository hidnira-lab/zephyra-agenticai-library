---
phase: 01-app-foundation-roles
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, package-lock.json, next.config.ts, tsconfig.json, eslint.config.mjs, postcss.config.mjs, docker-compose.yml, .env.example, prisma/schema.prisma, prisma/seed.ts, src/app/layout.tsx, src/app/page.tsx, src/app/catalog/page.tsx, src/app/librarian/dashboard/page.tsx, src/lib/db.ts, src/lib/seed-data.ts, src/styles/globals.css, tests/auth/seed-data.test.ts]
autonomous: true
requirements: [AUTH-04]
user_setup: []
must_haves:
  truths:
    - "The repository has a runnable Next.js application with dev, build, lint, and test scripts."
    - "Local PostgreSQL can run through Docker Compose using DATABASE_URL from .env.example."
    - "Prisma defines User and Book models that support Phase 1 auth and later catalog slices."
    - "Seed data includes one librarian account and sample books."
  artifacts:
    - path: "package.json"
      provides: "Next.js, Prisma, lint, build, and test scripts"
      contains: "\"dev\""
    - path: "docker-compose.yml"
      provides: "Local PostgreSQL service"
      contains: "postgres"
    - path: "prisma/schema.prisma"
      provides: "User, Role, and Book schema"
      contains: "model User"
    - path: "src/lib/db.ts"
      provides: "Shared Prisma client"
      contains: "PrismaClient"
    - path: "src/app/catalog/page.tsx"
      provides: "Student landing placeholder"
      min_lines: 10
    - path: "src/app/librarian/dashboard/page.tsx"
      provides: "Librarian landing placeholder"
      min_lines: 10
  key_links:
    - from: "prisma/seed.ts"
      to: "prisma/schema.prisma"
      via: "Creates records matching User and Book models"
      pattern: "prisma\\.(user|book)\\.upsert"
    - from: "package.json"
      to: "prisma/seed.ts"
      via: "Seed script command"
      pattern: "db:seed"
---

<objective>
Scaffold the Library Management System application foundation and prove the first full-stack slice.

Purpose: Establish the Next.js/PostgreSQL skeleton that every later phase will extend.
Output: Next.js app structure, Prisma schema, Docker Compose PostgreSQL, seed data, baseline scripts, and placeholder role landing routes.
Decision coverage: D-10 seed one librarian and sample books; D-11 establish a clean school-library dashboard style; D-12 include dev, build, lint, and focused test scripts; D-13 run local PostgreSQL through Docker Compose.
</objective>

<execution_context>
@C:/Users/hiday/WebstormProjects/zephyra-agenticai-library/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/hiday/WebstormProjects/zephyra-agenticai-library/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-app-foundation-roles/01-CONTEXT.md
@.planning/phases/01-app-foundation-roles/01-RESEARCH.md
@.planning/phases/01-app-foundation-roles/SKELETON.md
@.planning/codebase/STACK.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the Next.js app scaffold and scripts</name>
  <files>package.json, package-lock.json, next.config.ts, tsconfig.json, eslint.config.mjs, postcss.config.mjs, src/app/layout.tsx, src/app/page.tsx, src/styles/globals.css</files>
  <read_first>package.json, package-lock.json, .planning/phases/01-app-foundation-roles/01-CONTEXT.md</read_first>
  <action>Install and configure a minimal Next.js App Router application using TypeScript. Add scripts: dev, build, lint, test, test:e2e, db:generate, db:migrate, db:seed, db:reset. Configure global CSS with a clean school-library dashboard style, not a marketing landing page. The root page should redirect or link users toward login/register without implementing auth yet.</action>
  <verify>npm run lint and npm run build complete after dependencies are installed.</verify>
  <acceptance_criteria>
    - package.json contains scripts named "dev", "build", "lint", "test", "test:e2e", "db:generate", "db:migrate", "db:seed", and "db:reset".
    - src/app/layout.tsx exports metadata for "Library Management System".
    - src/styles/globals.css exists and defines usable base styling.
  </acceptance_criteria>
  <done>The app scaffold builds and the required scripts exist.</done>
</task>

<task type="auto">
  <name>Task 2: Add PostgreSQL, Prisma schema, and seed data</name>
  <files>docker-compose.yml, .env.example, prisma/schema.prisma, prisma/seed.ts, src/lib/db.ts, src/lib/seed-data.ts, tests/auth/seed-data.test.ts</files>
  <read_first>.planning/REQUIREMENTS.md, .planning/phases/01-app-foundation-roles/01-RESEARCH.md</read_first>
  <action>Add Docker Compose PostgreSQL with service name "postgres", database "library_mgmt", user "library_user", and password "library_password". Add .env.example with DATABASE_URL, SESSION_SECRET, LIBRARIAN_EMAIL, LIBRARIAN_PASSWORD, and LIBRARIAN_NAME. Create Prisma models Role enum, User, and Book. Add a shared Prisma client in src/lib/db.ts. Add seed defaults in src/lib/seed-data.ts and prisma/seed.ts that upserts one librarian account and at least three books. Hash the librarian password in seed.ts using bcryptjs.</action>
  <verify>npm run db:generate and npm test -- tests/auth/seed-data.test.ts complete after dependencies are installed.</verify>
  <acceptance_criteria>
    - docker-compose.yml contains a postgres service with a healthcheck.
    - .env.example contains DATABASE_URL and SESSION_SECRET.
    - prisma/schema.prisma contains enum Role with STUDENT and LIBRARIAN.
    - prisma/schema.prisma contains model User and model Book.
    - prisma/seed.ts calls prisma.user.upsert and prisma.book.upsert or createMany with duplicate handling.
    - tests/auth/seed-data.test.ts verifies the default librarian and sample book data shape.
  </acceptance_criteria>
  <done>Local database configuration, schema, seed data, and seed tests are present.</done>
</task>

<task type="auto">
  <name>Task 3: Add role landing placeholders backed by the scaffold</name>
  <files>src/app/catalog/page.tsx, src/app/librarian/dashboard/page.tsx, src/app/page.tsx</files>
  <read_first>src/app/layout.tsx, src/styles/globals.css, .planning/phases/01-app-foundation-roles/SKELETON.md</read_first>
  <action>Create a student catalog placeholder at /catalog and librarian dashboard placeholder at /librarian/dashboard. These pages should make later destinations visible without implementing later phase features: student page references catalog and my borrowings placeholder; librarian page references dashboard, catalog management placeholder, and borrow requests placeholder. Keep copy concise and mark later-phase areas as placeholders.</action>
  <verify>npm run build succeeds.</verify>
  <acceptance_criteria>
    - src/app/catalog/page.tsx contains "Catalog" and "My Borrowings".
    - src/app/librarian/dashboard/page.tsx contains "Dashboard", "Catalog Management", and "Borrow Requests".
    - Pages do not implement catalog CRUD, search, borrowing, returns, overdue logic, or statistics.
  </acceptance_criteria>
  <done>Role landing placeholders compile and clearly defer later-phase features.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run lint
- [ ] npm run build
- [ ] npm test -- tests/auth/seed-data.test.ts
- [ ] npm run db:generate
</verification>

<success_criteria>
- All tasks completed.
- Next.js app scaffold exists and builds.
- Docker Compose and Prisma establish PostgreSQL persistence.
- User and Book schema plus seed data exist.
- Role landing placeholders exist without implementing later-phase workflows.
</success_criteria>

<output>
After completion, create `.planning/phases/01-app-foundation-roles/01-01-SUMMARY.md`
</output>
