---
phase: 01-app-foundation-roles
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [src/lib/auth/password.ts, src/lib/auth/session.ts, src/lib/auth/guards.ts, src/app/(auth)/login/page.tsx, src/app/(auth)/register/page.tsx, src/components/auth/login-form.tsx, src/components/auth/register-form.tsx, src/app/actions/auth.ts, src/app/catalog/page.tsx, src/app/librarian/dashboard/page.tsx, tests/auth/password.test.ts, tests/auth/session.test.ts]
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03]
user_setup: []
must_haves:
  truths:
    - "A student can register with name, email, and password."
    - "A registered student can log in and log out."
    - "A seeded librarian can log in and log out."
    - "Successful student login lands on /catalog."
    - "Successful librarian login lands on /librarian/dashboard."
  artifacts:
    - path: "src/app/(auth)/register/page.tsx"
      provides: "Student registration page"
      min_lines: 20
    - path: "src/app/(auth)/login/page.tsx"
      provides: "Shared login page"
      min_lines: 20
    - path: "src/app/actions/auth.ts"
      provides: "Server actions for register, login, and logout"
      contains: "registerStudent"
    - path: "src/lib/auth/session.ts"
      provides: "HTTP-only signed session cookie helpers"
      contains: "library_session"
    - path: "src/lib/auth/password.ts"
      provides: "Password hash and verify helpers"
      contains: "bcrypt"
  key_links:
    - from: "src/app/actions/auth.ts"
      to: "prisma.user"
      via: "Registration and login query persisted users"
      pattern: "prisma\\.user\\.(create|findUnique)"
    - from: "src/app/actions/auth.ts"
      to: "src/lib/auth/session.ts"
      via: "Successful auth creates session cookie"
      pattern: "createSession"
    - from: "src/app/(auth)/login/page.tsx"
      to: "src/app/actions/auth.ts"
      via: "Login form submits to server action"
      pattern: "login"
---

<objective>
Implement student registration, student login/logout, librarian login/logout, and session handling.

Purpose: Satisfy the core Phase 1 authentication requirements using the database foundation from Plan 01.
Output: Auth forms, server actions, password helpers, session helpers, redirects, and focused auth helper tests.
Decision coverage: D-01 students self-register; D-02 librarians are seeded/admin-created and not publicly self-registered; D-03 students use email/password; D-04 email verification is excluded from v1; D-05 one default librarian account is seeded; D-06 post-login redirects send students to catalog and librarians to dashboard.
</objective>

<execution_context>
@C:/Users/hiday/WebstormProjects/zephyra-agenticai-library/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/hiday/WebstormProjects/zephyra-agenticai-library/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-app-foundation-roles/01-CONTEXT.md
@.planning/phases/01-app-foundation-roles/01-RESEARCH.md
@.planning/phases/01-app-foundation-roles/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement password and session helpers</name>
  <files>src/lib/auth/password.ts, src/lib/auth/session.ts, src/lib/auth/guards.ts, tests/auth/password.test.ts, tests/auth/session.test.ts</files>
  <read_first>prisma/schema.prisma, src/lib/db.ts, .env.example, .planning/phases/01-app-foundation-roles/01-CONTEXT.md</read_first>
  <action>Create password helpers using bcryptjs with exported functions hashPassword(password: string) and verifyPassword(password: string, hash: string). Create session helpers using jose and Next.js cookies with cookie name "library_session"; include createSession(user), getCurrentUser(), and clearSession(). Create guard helper getPostLoginRedirect(role) returning "/catalog" for STUDENT and "/librarian/dashboard" for LIBRARIAN. Add Vitest coverage for password verify success/failure and session payload validation where feasible without relying on browser cookies.</action>
  <verify>npm test -- tests/auth/password.test.ts tests/auth/session.test.ts</verify>
  <acceptance_criteria>
    - password.ts exports hashPassword and verifyPassword.
    - session.ts contains "library_session" and signs sessions with SESSION_SECRET.
    - guards.ts maps STUDENT to /catalog and LIBRARIAN to /librarian/dashboard.
    - Password tests cover correct and incorrect passwords.
  </acceptance_criteria>
  <done>Auth helper modules exist and tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Build registration and login server actions</name>
  <files>src/app/actions/auth.ts, src/app/(auth)/register/page.tsx, src/components/auth/register-form.tsx, src/app/(auth)/login/page.tsx, src/components/auth/login-form.tsx</files>
  <read_first>src/lib/auth/password.ts, src/lib/auth/session.ts, src/lib/auth/guards.ts, prisma/schema.prisma</read_first>
  <action>Add server actions registerStudent, loginUser, and logoutUser. registerStudent must create only STUDENT users, reject duplicate email, hash the password, create a session, and redirect to /catalog. loginUser must verify email/password for both STUDENT and LIBRARIAN roles, create a session, and redirect with getPostLoginRedirect. logoutUser must clear the cookie and redirect to /login. Add simple form components that display validation errors returned from server actions. Do not add public librarian registration.</action>
  <verify>npm run build</verify>
  <acceptance_criteria>
    - registerStudent creates users with role STUDENT only.
    - loginUser can authenticate a seeded LIBRARIAN and a registered STUDENT.
    - logoutUser clears the session cookie.
    - Register and login pages use the server actions and have accessible labels for email and password.
  </acceptance_criteria>
  <done>Registration, login, and logout flows compile and use persisted users.</done>
</task>

<task type="auto">
  <name>Task 3: Surface authenticated user state on role pages</name>
  <files>src/app/catalog/page.tsx, src/app/librarian/dashboard/page.tsx</files>
  <read_first>src/lib/auth/session.ts, src/app/actions/auth.ts, src/app/catalog/page.tsx, src/app/librarian/dashboard/page.tsx</read_first>
  <action>Update the student catalog and librarian dashboard placeholders to read the current session user and show the user's name/email plus a logout control wired to logoutUser. Keep later-phase functionality as placeholders. If no session is present, redirect to /login.</action>
  <verify>npm run build</verify>
  <acceptance_criteria>
    - /catalog reads getCurrentUser and redirects anonymous users to /login.
    - /librarian/dashboard reads getCurrentUser and redirects anonymous users to /login.
    - Both pages render a logout form or button wired to logoutUser.
  </acceptance_criteria>
  <done>Authenticated role pages show real session state and support logout.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run lint
- [ ] npm run build
- [ ] npm test -- tests/auth/password.test.ts tests/auth/session.test.ts
- [ ] Manual or scripted check: student register -> /catalog, librarian login -> /librarian/dashboard, logout -> /login
</verification>

<success_criteria>
- Student registration works with persisted PostgreSQL users.
- Student login/logout works.
- Seeded librarian login/logout works.
- Sessions are stored in signed HTTP-only cookies.
- No public librarian registration exists.
</success_criteria>

<output>
After completion, create `.planning/phases/01-app-foundation-roles/01-02-SUMMARY.md`
</output>
